"""Gmail integration package.

Real Gmail is treated as an external input source. It fetches messages and
attachments, persists them as ordinary ShipSync emails, and then hands them to
the existing workflow.
"""
